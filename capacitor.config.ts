import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'client.bmgmt.com',
  appName: 'builder-mgmt',
  webDir: 'dist/builder-mgmt',
  bundledWebRuntime: false
};

export default config;
